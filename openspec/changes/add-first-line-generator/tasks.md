## 1. Подготовка набора реплик

- [x] 1.1 Определить фиксированный набор тонов (категорий) и их подписи для интерфейса — одно слово на тон.
- [x] 1.2 Создать `tools/first-line/lines.js` с единообразной структурой записи: текст реплики и один тон-тег.
- [x] 1.3 Составить не менее 200 первых реплик на русском языке, сбалансировав их по тонам.
- [x] 1.4 Вручную проверить весь список, удалить смысловые дубли и реплики, навязывающие персонажей, сюжет или развязку сверх исходной ситуации.
- [x] 1.5 Проверить, что каждой реплике присвоен корректный тон из фиксированного набора.

## 2. Самостоятельный интерфейс «Софит»

- [x] 2.1 Создать `tools/first-line/index.html` с областью реплики, подписью тона, показом прогресса, крупным фильтром тонов, основной кнопкой и подписанной ссылкой в каталог.
- [x] 2.2 Создать локальное оформление образа «Софит» (тёмная сцена, мягкий луч, крупная центральная реплика, минимум деталей), не повторяющее дизайн других инструментов.
- [x] 2.3 Реализовать анимацию появления реплики «на свету» и отклик элементов управления, не блокирующие следующее действие.
- [x] 2.4 Добавить мягкий звук при получении новой реплики, работающий на десктопе и мобильных.
- [x] 2.5 Адаптировать компоновку для телефона и настольного экрана, добавить видимые состояния фокуса, `aria-live` и `touch-action: manipulation` на всех тап-целях.

## 3. Логика генерации и фильтра

- [x] 3.1 Реализовать в `tools/first-line/app.js` перемешанную колоду без повторов и защиту от немедленного повтора между циклами.
- [x] 3.2 Реализовать фильтр по тону: выбор «все» или одного тона задаёт активную колоду и начинает её заново.
- [x] 3.3 Реализовать показ прогресса — сколько реплик показано из общего числа в текущей колоде, с обнулением при смене фильтра и начале нового цикла.
- [x] 3.4 Показывать тон-тег выбранной реплики вместе с её текстом.

## 4. Интеграция в каталог

- [x] 4.1 Подготовить отдельный графический идентификатор генератора первой реплики в `shared/assets/`.
- [x] 4.2 Добавить в корневой каталог полноразмерную карточку «Первая реплика» с относительной ссылкой на новую страницу.

## 5. Проверка

- [x] 5.1 Статически проверить JavaScript, обязательные поля, количество записей (не менее 200), отсутствие точных дублей и корректность тонов.
- [x] 5.2 Проверить через `file://` загрузку страницы, первый запуск, последовательную генерацию, отсутствие повторов, новый цикл, работу фильтра по тону и корректность прогресса.
- [ ] 5.3 Проверить каталог и инструмент на телефоне и настольном экране с указателем, клавиатурой и касанием, включая фокус, объявление результата, плавность анимаций, звук и возможность быстро запускать следующую генерацию.
- [x] 5.4 Выполнить `git diff --check` и строгую валидацию OpenSpec change.
