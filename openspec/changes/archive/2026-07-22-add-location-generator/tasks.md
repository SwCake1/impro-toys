## 1. Данные локаций

- [x] 1.1 Сконвертировать `tools/locations/locations.txt` в `tools/locations/locations.js` с `window.LOCATIONS = [...]` (массив строк, все 104 локации, без пустых и дублей)
- [x] 1.2 Определиться с судьбой `locations.txt` (оставить как исходник или удалить) и зафиксировать выбор — оставлен как человекочитаемый исходник рядом с `locations.js`

## 2. Разметка и подключение

- [x] 2.1 Создать `tools/locations/index.html` с навигацией «Все инструменты», областью табло, счётчиком локаций, основной кнопкой и подсказкой по клавишам
- [x] 2.2 Подключить `locations.js` и `app.js` тегами `<script>` по относительным путям; добавить `<noscript>`-сообщение
- [x] 2.3 Задать `aria-live` для области локации и доступные подписи кнопок

## 3. Логика генерации

- [x] 3.1 Реализовать перемешанный перебор без повторов внутри круга (перемешать → идти по индексу → по исчерпании перемешать заново)
- [x] 3.2 Привязать основную кнопку, клавиши (пробел/Enter/стрелка вправо), касание и указатель к генерации следующей локации
- [x] 3.3 Показать стартовое состояние, готовое к первой генерации без настройки
- [x] 3.4 Выводить счётчик локаций «позиция из общего числа» (например «17 из 104»), обновлять при каждой смене, общее число брать из размера подборки

## 4. Split-flap табло

- [x] 4.1 Сверстать табло из ячеек-барабанов, вмещающее самую длинную локацию, с переносом слов и символов на узких экранах
- [x] 4.2 Реализовать анимацию перекручивания символов до целевого текста с волной задержки по колонкам (кириллица + пробел + дефис, ограниченная длительность листания)
- [x] 4.3 Гарантировать читаемое статичное финальное состояние текста локации
- [x] 4.4 Учесть `prefers-reduced-motion: reduce` — сократить/отключить анимацию, показывать локацию сразу
- [x] 4.5 Оформить самостоятельный стиль табло (вынесено в `tools/locations/styles.css`)

## 5. Каталог

- [x] 5.1 Добавить иконку карточки в `shared/assets/` (`locations.svg`)
- [x] 5.2 Добавить запускаемую карточку «Генератор локаций» в корневой `index.html` с относительной ссылкой и собственным акцентным цветом

## 6. Проверка

- [x] 6.1 Открыть инструмент через `file://` — убедиться, что локации загружаются, генерация и переход в каталог работают без сети
- [x] 6.2 Проверить адаптивность на телефоне и десктопе: самая длинная локация, отсутствие горизонтальной прокрутки
- [x] 6.3 Проверить указатель, касание и клавиатуру, видимый фокус и объявление новой локации вспомогательным технологиям
- [x] 6.4 Проверить поведение при `prefers-reduced-motion: reduce`
