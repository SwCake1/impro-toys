## 1. Подготовка набора реплик

- [x] 1.1 Определить фиксированный набор тем (категорий) и их подписи для интерфейса — одно слово на тему.
- [x] 1.2 Создать `tools/first-line/lines.js` с единообразной структурой записи: текст реплики и один тег темы.
- [x] 1.3 Составить не менее 200 первых реплик на русском языке, сбалансировав их по темам.
- [x] 1.4 Вручную проверить весь список, удалить смысловые дубли и реплики, навязывающие персонажей, сюжет или развязку сверх исходной ситуации.
- [x] 1.5 Проверить, что каждой реплике присвоена корректная тема из фиксированного набора.

## 2. Самостоятельный интерфейс «Софит»

- [x] 2.1 Создать `tools/first-line/index.html` с областью реплики, подписью темы, показом прогресса, кнопкой настроек тем, основной кнопкой и подписанной ссылкой в каталог.
- [x] 2.2 Создать локальное оформление образа «Софит» (тёмная сцена, мягкий луч, крупная центральная реплика, минимум деталей), не повторяющее дизайн других инструментов.
- [x] 2.3 Реализовать анимацию появления реплики «на свету» и отклик элементов управления, не блокирующие следующее действие.
- [x] 2.4 Добавить мягкий звук при получении новой реплики, работающий на десктопе и мобильных.
- [x] 2.5 Адаптировать компоновку для телефона и настольного экрана, добавить видимые состояния фокуса, `aria-live` и `touch-action: manipulation` на всех тап-целях.

## 3. Логика генерации и настройки тем

- [x] 3.1 Реализовать в `tools/first-line/app.js` перемешанную колоду без повторов и защиту от немедленного повтора между циклами.
- [x] 3.2 Реализовать настройку тем: модальная панель с чекбоксом на каждую тему, все включены по умолчанию, изменение набора задаёт активную колоду и начинает её заново.
- [x] 3.3 Реализовать показ прогресса — сколько реплик показано из общего числа в текущей колоде, с обнулением при изменении набора включённых тем и начале нового цикла.
- [x] 3.4 Показывать тег темы выбранной реплики вместе с её текстом.

## 4. Интеграция в каталог

- [x] 4.1 Подготовить отдельный графический идентификатор генератора первой реплики в `shared/assets/`.
- [x] 4.2 Добавить в корневой каталог полноразмерную карточку «Первая реплика» с относительной ссылкой на новую страницу.

## 5. Проверка

- [x] 5.1 Статически проверить JavaScript, обязательные поля, количество записей (не менее 200), отсутствие точных дублей и корректность тем.
- [x] 5.2 Проверить через `file://` загрузку страницы, первый запуск, последовательную генерацию, отсутствие повторов, новый цикл, работу настройки тем и корректность прогресса.
- [x] 5.3 Проверить каталог и инструмент на телефоне и настольном экране с указателем, клавиатурой и касанием, включая фокус, объявление результата, плавность анимаций, звук и возможность быстро запускать следующую генерацию.
- [x] 5.4 Выполнить `git diff --check` и строгую валидацию OpenSpec change.
